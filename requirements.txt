# 依据 conda 环境 gp_gui_zhuanli 的 `pip freeze` 整理：
# - 已去掉仅本地路径安装、无法在 PyPI 复现的包（如 gp-gui-system @ file://...）
# - 已去掉 Jupyter/IPython 调试链等与本仓库 `main.py` 运行无直接关系的包
# - PyTorch / pygraphviz 见文末说明
#
# 安装：pip install -r requirements.txt
# 建议在新建 venv/conda 环境中安装。

# ----- 核心数值与 GP -----
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
joblib==1.5.2
threadpoolctl==3.6.0
deap==1.4.3

# ----- 树模型 / Boosting（main 中 residual_fitting 与 baseline） -----
xgboost==3.1.1
lightgbm==4.6.0
catboost==1.2.8

# ----- 解释性（shap.open=True、baseline/残差 SHAP） -----
shap==0.50.0
numba==0.63.0
llvmlite==0.46.0
cloudpickle==3.1.2
slicer==0.0.8

# ----- 可视化（树 PDF、评估图、Dataset_split 中作图） -----
matplotlib==3.10.7
seaborn==0.13.2
contourpy==1.3.3
cycler==0.12.1
fonttools==4.60.1
kiwisolver==1.4.9
pillow==12.0.0
pyparsing==3.2.5
graphviz==0.21
plotly==6.3.1

# ----- 深度学习（DNN baseline / 残差中的 DNN） -----
# 当前环境中为 torch==2.10.0.dev20251208+cu128（nightly + CUDA 12.8），一般机器无法直接 pip 到相同构建。
# 请按 https://pytorch.org/ 选择与你的 CUDA/CPU 匹配的命令安装；以下为 CPU/稳定版占位，可按需改写：
torch>=2.4,<2.11
sympy==1.14.0
networkx==3.6
mpmath==1.3.0
filelock==3.20.0
fsspec==2025.10.0
Jinja2==3.1.6
MarkupSafe==3.0.3

# ----- Streamlit 前端（utils/llm_new_feature/streamlit_prev 等） -----
streamlit==1.51.0
altair==5.5.0
blinker==1.9.0
cachetools==6.2.2
click==8.3.1
GitPython==3.1.45
gitdb==4.0.12
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
narwhals==2.12.0
packaging==24.2
protobuf==6.33.1
pyarrow==21.0.0
pydantic==2.12.4
pydantic_core==2.41.5
pydeck==0.9.1
referencing==0.37.0
requests==2.32.5
rpds-py==0.29.0
smmap==5.0.2
tenacity==9.1.2
toml==0.10.2
tornado>=6.0.3,<7
typing-inspection==0.4.2
typing_extensions>=4.12.0
urllib3==2.5.0
watchdog==6.0.0
attrs==25.4.0
annotated-types==0.7.0
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11
tzdata==2025.2
pytz==2025.2

# ----- 数据读写与 PDF（Dataset_split / LLM 工具链可能用到） -----
openpyxl==3.1.5
xlrd==2.0.2
et_xmlfile==2.0.0
pypdf==6.9.2
PyYAML==6.0.3

# ----- 其它工具 -----
tqdm==4.67.1
colorama>=0.4.6

# ----- pygraphviz（可选：与 graphviz Python 包不同；需系统已安装 Graphviz，Windows 上 wheel 因环境而异） -----
# 当前 conda 中为本地构建的 pygraphviz；若 `pip install pygraphviz` 失败，请用 conda-forge：
#   conda install -c conda-forge pygraphviz
# pygraphviz>=1.14
